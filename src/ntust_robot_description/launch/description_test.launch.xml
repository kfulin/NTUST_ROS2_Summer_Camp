<?xml version="1.0"?>
<launch>

  <let name="urdf_file"
       value="$(find-pkg-share ntust_robot_description)/urdf/urdf_test.urdf" />

  <!-- robot_state_publisher -->
  <node pkg="robot_state_publisher"
        exec="robot_state_publisher"
        name="robot_state_publisher">

    <param name="robot_description"
           value="$(file-content $(var urdf_file))"
           type="str" />

  </node>

  <!-- joint GUI -->
  <node pkg="joint_state_publisher_gui"
        exec="joint_state_publisher_gui"
        name="joint_state_publisher_gui"
        output="screen"/>

  <!-- RViz2 -->
  <node pkg="rviz2"
        exec="rviz2"
        name="rviz2"
        args="-d $(find-pkg-share ntust_robot_description)/config/config.rviz" />

</launch>